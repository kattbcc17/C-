#include <iostream>
using namespace std;
int main(){
    // int a = 8 , b = 2, add; // put in same line because they share the same type
    // int add;

    // add = a + b;

    // cout << "The result is: " << a % b;
    // cout << "The result is: " << add;

    int a = 10;
    int result_a;

    // incrementar variable a
    // result_a = ++a;
    result_a = --a;

    cout << "Now the variable is: " << result_a;

    return 0;
}